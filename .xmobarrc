Config
	{ 
		font = "xft:Terminus:bold:size=16",
		bgColor = "#000",
		fgColor = "#868686",
		position = Top,
		lowerOnStart = True,
		commands = 
			[ 
				Run Date "%y%m%_d %H:%M " "date" 10,
				Run StdinReader
			],
		sepChar = "%",
		alignSep = "}{",
		template = " %StdinReader% }{ <fc=#ffffff>%date%</fc>"
	}
